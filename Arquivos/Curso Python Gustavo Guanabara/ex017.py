# Hipotenusa
from math import sqrt

a = float(input('Digite o A'))
b = float(input('Digite o B'))

soma = a**2 + b**2
c = sqrt(soma) # REDUNDANTE MEMO KS, ta bonito
print('A Resultado a Hipotenusa é {} '.format(c))

